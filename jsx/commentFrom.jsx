var React = require('react');

var CommentForm = React.createClass({
  render: function() {
    return (
      <div className="commentForm">
        Hello, winty! I am a CommentForm.
      </div>
    );
  }
});

module.exports = CommentForm;